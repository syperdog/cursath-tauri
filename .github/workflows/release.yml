Да, папка cursath на скриншоте — это корень вашего репозитория. НО есть важный нюанс.

На скриншоте видно файл package-lock.json, но нет файла package.json.
Скорее всего, ваш проект (сам код Tauri и React/Vue) лежит внутри папки PRG, а внутри неё — в папке service_station (судя по логам из ваших прошлых скриншотов).

GitHub Actions "теряется", потому что он ищет файлы в главной папке, а они спрятаны глубже.

Что нужно сделать прямо сейчас

Вам нужно отредактировать файл .github/workflows/release.yml, указав правильный путь к папке с проектом.

Предполагаем (на основе прошлых логов), что путь такой: PRG/service_station.

Вот готовый код для вашего release.yml. Скопируйте его полностью и замените то, что у вас есть:

code
Yaml
download
content_copy
expand_less
name: Release
on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    strategy:
      matrix:
        include:
          - platform: "windows-latest"
            args: ""

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
      
      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          
      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
          
      - name: install frontend dependencies
        # 👇 ГЛАВНОЕ ИЗМЕНЕНИЕ 1: Говорим зайти в папку перед установкой
        working-directory: ./PRG/service_station
        run: npm install
        
      - name: Build the app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          # 👇 ГЛАВНОЕ ИЗМЕНЕНИЕ 2: Говорим Tauri, где искать конфиги
          projectPath: ./PRG/service_station
          
          tagName: v__VERSION__ 
          releaseName: "App v__VERSION__"
          releaseBody: "See the assets to download this version and install."
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
Как применить изменения

Отредактируйте файл release.yml у себя на компьютере (вставьте код выше).

Выполните команды в терминале:

code
Bash
download
content_copy
expand_less
# Сохраняем исправление путей
git add .github/workflows/release.yml
git commit -m "Исправил пути к папке PRG/service_station"
git push origin main

# Запускаем новую попытку сборки (Новый тег)
git tag v4
git push origin v4

Если вдруг внутри папки PRG папка называется НЕ service_station, замените это название в коде выше на правильное.
